# Credit Card Fraud Detection — Preprocessing Workflow

## Project Overview

This folder contains the preprocessing workflow for our Data Mining coursework on **Credit Card Fraud Detection** using the **MLG-ULB credit card fraud dataset**.

The purpose of this part of the project is to create a reproducible and well-documented preprocessing pipeline that can be used by all team members before model training.

This preprocessing workflow includes:

- loading the raw dataset
- validating the dataset structure
- checking missing values and duplicates
- removing exact duplicate rows
- analysing class imbalance
- inspecting important raw features
- creating a leakage-safe stratified train/test split
- preparing two preprocessing pathways:
  - minimal / unscaled
  - robust-scaled
- exporting processed datasets for later modelling

---

## Folder Structure

```text
DM_CW/
│
├── data/
│   └── creditcard.csv
│
├── notebooks/
│   └── preprocessing.ipynb
│
├── outputs/
│   └── preprocessed/
│
├── requirements.txt
└── README.md
```

### Explanation of each component

| Item | Description |
|------|-------------|
| `data/` | Contains the raw dataset file |
| `notebooks/` | Contains the preprocessing notebook |
| `outputs/preprocessed/` | Stores the processed datasets generated by the notebook |
| `requirements.txt` | Lists the Python packages required to run the project |
| `README.md` | Contains project documentation and execution instructions |

---

## Dataset Instructions

This project uses the **MLG-ULB Credit Card Fraud Detection dataset**.

**Expected file name**

```text
creditcard.csv
```

**Expected location**

```text
DM_CW/data/creditcard.csv
```

---

## Environment Setup

It is recommended to run the notebook inside a **separate Python virtual environment** to avoid dependency conflicts.

### Step 1 — Open the project folder

Navigate to the project root directory:

```bash
cd DM_CW
```

### Step 2 — Create a virtual environment

```bash
python3 -m venv fraud_env
```

### Step 3 — Activate the environment

#### macOS / Linux

```bash
source fraud_env/bin/activate
```

#### Windows

```bash
fraud_env\Scripts\activate
```

### Step 4 — Install required packages

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Step 5 — Register a new Jupyter kernel

```bash
python -m ipykernel install --user --name fraud_env --display-name "Fraud Detection Env"
```

### Step 6 — Launch Jupyter Notebook

```bash
jupyter notebook
```

Then open:

```text
notebooks/preprocessing.ipynb
```

Select the kernel:

```text
Fraud Detection Env
```

---

## Dataset Path Explanation

The notebook is stored inside the `notebooks/` directory while the dataset is stored inside the `data/` directory.

Because of this, the notebook loads the dataset using the following relative path:

```python
DATA_PATH = Path("../data/creditcard.csv")
```

This means the notebook moves one directory up from `notebooks/` and then accesses the dataset inside the `data/` folder.

---

## Output Description

After the notebook finishes running, the following files will be created in:

```text
DM_CW/outputs/preprocessed/
```

### Expected files

```text
X_train_minimal.csv
X_test_minimal.csv
X_train_robust_scaled.csv
X_test_robust_scaled.csv
y_train.csv
y_test.csv
preprocessing_metadata.json
```

---

## Modelling Usage Guidance

Different models should use different preprocessing pathways.

### Minimal Dataset (Unscaled)

Recommended for:

- Random Forest
- XGBoost
- LightGBM
- Isolation Forest

**Files to use**

```text
X_train_minimal.csv
X_test_minimal.csv
```

### Robust-Scaled Dataset

Recommended for:

- Logistic Regression
- One-Class SVM

**Files to use**

```text
X_train_robust_scaled.csv
X_test_robust_scaled.csv
```

### Target Labels

All models should use:

```text
y_train.csv
y_test.csv
```

---

## Reproducibility Notes

This preprocessing workflow follows several best practices to ensure reproducibility:

- a fixed random seed is used for consistent results
- train/test splitting is stratified to preserve class distribution
- cross-validation uses `StratifiedKFold`
- scaling is fitted only on the training set to prevent data leakage
- duplicate rows are removed to avoid repeated identical observations
- metadata describing preprocessing decisions is saved to a JSON file

These practices ensure that experiments across the project team remain consistent, traceable, and methodologically sound.
